import { MessageOnErrorDirective } from './message-on-error.directive';

describe('MessageOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
